import React from 'react';
import ReactDOM from 'react-dom/client';
import './css/tailwind.css';
import TazkeerApp from './TazkeerApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TazkeerApp />
  </React.StrictMode>
);
